export GIT_EXCLUDE_FILES="${HOME}/.git/ignore"
export GIT_HOOKS_PATH="${HOME}/.git/hooks"
git() {
  hub -c core.excludesFile="${GIT_EXCLUDES_FILE}" -c core.hooksPath="${GIT_HOOKS_PATH}" "$@"
  return $?
}
