#!/bin/bash
usage ()
{
  echo 'Usage : mkpackagedirs <package>'
  echo 'package - name of java package'
  exit 1
}
if [ -z $1 ]
  then
  usage
else
  dir=$(echo $1 | sed -e 's/\./\//g')
  echo $dir
  mkdir -p $dir
  exit $?
fi
